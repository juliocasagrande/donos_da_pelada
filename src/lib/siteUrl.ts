// Canonical public domain. Links shared with users (WhatsApp, copy invite,
// etc.) must always point here instead of the Railway-generated host, so
// they read as our own domain regardless of which host served the request.
export const SITE_URL = "https://donosdapelada.com";
